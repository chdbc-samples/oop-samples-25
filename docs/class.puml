classDiagram
  class BonusEligible {
    <<interface>>
    +double DEFAULT_BONUS$
    +getBonus() double
    +setBonus(double bonus) void
  }

  class Person {
    <<Abstract>>
    -String name
    +Person()
    +Person(String name)
    +getName() String
    +setName(String name) void
    +displayInfo()* void
    +getFullDetails() String
    +printInfo(Person person)$ void
  }

  class Employee {
    -double bonus
    +Employee()
    +Employee(String name)
    +Employee(String name, double bonus)
    +getBonus() double
    +setBonus(double bonus) void
    +displayInfo() void
  }

  class Paycheck {
    +double amount
    +String payDate
    +Paycheck(double amount, String payDate)
    +displayPaycheckInfo() void
  }

  class Payroll {
    -List~Person~ employees
    -Paycheck paycheck
    +setEmployees(List~Person~ employees) void
    +processPayroll(double salary) void
  }

  class PayrollModule {
    <<Guice Module>>
    +provideEmployees() List~Person~
  }

  BonusEligible <|.. Employee : implements
  Person <|-- Employee : extends
  Payroll o-- Person : aggregates
  Payroll *-- Paycheck : composition
  PayrollModule ..> Employee : creates
  PayrollModule ..> Person : provides